{
  "_comment": "Copy the `mcpServers` block into your ~/Library/Application Support/Claude/claude_desktop_config.json (Mac) or %APPDATA%/Claude/claude_desktop_config.json (Windows), then restart Claude Desktop. You should see 4 new tools available. Powered by OPDSTAR (https://opdstar.com).",
  "mcpServers": {
    "opdstar-nhi": {
      "command": "npx",
      "args": ["-y", "@opdstar/nhi-mcp"]
    }
  }
}
